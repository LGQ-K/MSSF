Users can divide the dataset by themselves according to their needs. 
We only divide the training set and the test set (val set). 
The dataset contains 7757 frames, 11 of which have no objectives, 
and are divided into the training set.
